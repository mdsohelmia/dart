void main()
{
  //Number : int
  int score = 10;
  var count = 20; //It is inferred as integer automatically.
  print(score);
  int hexValue = 0xFFFFFF;
  print(hexValue);

  //Number :double
    double amount = 10.2;
    print(amount);

    //String;
    String countryName = 'Bangladesh';
    print(countryName);

    //Boolean
    bool isLikable = false;
    print(isLikable);

    //Note :All data type in dart are objects.
}
